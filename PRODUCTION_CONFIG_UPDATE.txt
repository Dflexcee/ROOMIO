# ============================================================================
# FILE TO UPDATE ON SERVER: public_html/php-api/lib/Config.php
# Find lines 30-44 and REPLACE the array values
# ============================================================================

FIND THESE LINES (around line 30-44):

        self::$config = array_merge([
            'DB_HOST' => 'localhost',
            'DB_NAME' => 'roomio',
            'DB_USER' => 'root',
            'DB_PASS' => '',
            'APP_URL' => 'http://localhost/roomio/php-api/public',
            'UPLOAD_BASE_URL' => 'http://localhost/roomio/php-api/uploads',
            'CORS_ORIGINS' => 'http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:5175,http://127.0.0.1:5176',
            'ADMIN_EMAIL' => 'admin@roomio.com',
            'SUPPORT_EMAIL' => 'support@roomio.com',
            'SUPPORT_PHONE' => '+234 123 456 7890',
            'SESSION_SECURE' => 'false',
            'SESSION_HTTPONLY' => 'false',
            'SESSION_SAMESITE' => 'Lax'
        ], self::$config);

REPLACE WITH (PRODUCTION VALUES):

        self::$config = array_merge([
            'DB_HOST' => 'localhost',
            'DB_NAME' => 'grampaxg_roomio',
            'DB_USER' => 'grampaxg_roomiouser',
            'DB_PASS' => 'F+R)LZ62aM@D_7tO',
            'APP_URL' => 'https://collins.alonaccess.com/php-api/public',
            'UPLOAD_BASE_URL' => 'https://collins.alonaccess.com/php-api/uploads',
            'CORS_ORIGINS' => 'https://collins.alonaccess.com',
            'ADMIN_EMAIL' => 'admin@collins.alonaccess.com',
            'SUPPORT_EMAIL' => 'support@collins.alonaccess.com',
            'SUPPORT_PHONE' => '+234 123 456 7890',
            'SESSION_SECURE' => 'true',
            'SESSION_HTTPONLY' => 'true',
            'SESSION_SAMESITE' => 'Strict'
        ], self::$config);

# ============================================================================
# IMPORTANT: 
# - Only change lines 32-43 (inside the array)
# - Keep everything else exactly the same
# - Make sure you don't break the PHP syntax
# ============================================================================
